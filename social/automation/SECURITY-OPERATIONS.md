# Production operations — 2026-09-11

Dashboard: https://mindvortex.pro/studio-social. A separate VPS service, not a local development server. Password is unchanged; Basic Auth is replaced by password plus one-time email code. No username field. SMTP is addressed only to the configured owner. Session expires after eight hours and logout revokes it in PostgreSQL. Password stored as salted scrypt hash; OTP expires in ten minutes, five attempts; password attempts limited per proxy-verified IP; session and challenge cookies Secure, HttpOnly, SameSite=Strict.

Dedicated mv-social OS account; root-owned application code; writable media/state only; systemd sandbox and resource limits. PostgreSQL/Next listen on localhost. UFW permits HTTPS/HTTP and rate-limited SSH. Nginx limits requests, denies framing, sets CSP and no-store. sharp updated to 0.35.4; dependency audit zero reported vulnerabilities at check time. This is not a guarantee of absence of vulnerabilities.

Worker runs continuously with autopilot enabled at 10:00 Europe/Warsaw. Sync all Instagram history before generation and publication. Existing posts live in social_history, avoiding the schedule's one-post-per-day constraint. Full historical captions and locally verified notes participate in exact and semantic duplicate checks. Missing captions remain visibly missing; editorial notes are not represented as original captions.

Waiting duplicates are replaced automatically in the same date slot. Candidate generation has four attempts per job; jobs retry up to three times at 15-minute intervals. Previous copy/image retained in social_revisions. An uncertain Instagram publish request is NEVER blindly retried. AI checks failing closed can stop a post; these failures are visible and alerted.

Visual registry lib/visuals.mjs tracks website+section lineage. Hero desktop/mobile/crops share a source key. Different reviewed sections are allowed. SHA-256, perceptual similarity and image comparison against imported Instagram history help reject reused images. Pending slots reserve their assets. New section assets must be reviewed and registered; no unused section means a fresh text-only topic. AI comparisons are imperfect, not a guarantee.

Daily encrypted backup at 03:15 server time, 14-day retention, /var/backups/mindvortex-social. Recovery key /root/mindvortex-social-backup.key. Database restoration into an isolated temporary database tested successfully. One encrypted copy and recovery key copied to the owner's local ignored .env.social-backup* files. Recurring off-server replication is NOT configured. Monitor every five minutes checks public login, worker heartbeat and disk space; owner email on failure/recovery. Same-host monitoring cannot report total VPS/network loss; external uptime monitoring is not configured.

Deploy: upload explicit source archive as /home/ubuntu/.social-deploy.tar.gz and deploy-secure.sh; run sudo bash /home/ubuntu/deploy-secure.sh. After migrations/review, start mindvortex-social-worker. Never replace hardened environment with .env.social.local. Build runs on Linux; current -> timestamped release, root:mv-social. Do not deploy qa credentials, private env files or backup keys.

Checks: unit security/content/render tests; real database OTP failure, one-time success, replay and revocation; isolated database automatic duplicate replacement retry, unchanged slot and revision history; production history/queue checks; responsive login and PL/EN contact link checks. First scheduled 10:00 publish, future email reports and complete owner email-code browser login still require observation.
