import {pool} from './lib/db.mjs';
import {validate} from './lib/brand.mjs';
import {render} from './lib/render.mjs';
import {requireDistinct} from './lib/duplicates.mjs';
const replacements={
 '2026-09-13':{topic:'One identity across everyday touchpoints',headline:'ONE BRAND MANY PLACES',points:['Your website','Your social profile','Your everyday documents'],project:'none',caption:'A visual identity has to work in more than one place. A logo that looks clear across a website header also needs a useful version for a small social avatar. Documents introduce another set of sizes and backgrounds.\n\nWhen planning a visual identity with our studio, bring examples of the places you actually use your brand. That helps define which variations and files the work needs to include.\n\nBuilding an identity for your business? Tell us where it needs to appear at mindvortex.pro/en.'},
 '2026-09-14':{topic:'Writing useful image descriptions',headline:'DESCRIBE WHAT MATTERS',points:['Explain the image purpose','Skip decorative details','Avoid repeating nearby text'],project:'none',caption:'Alternative text should communicate the purpose of an informative image in its particular context. A product photograph might need a description of a feature that the surrounding text does not explain.\n\nA purely decorative image usually needs an empty alt attribute, so a screen reader can skip it. If the same information already appears beside the image, avoid making someone hear it twice.\n\nReview one page of your website: which images add information, and which only set the mood? Make that distinction before writing their descriptions.'},
 '2026-09-15':{topic:'Kierunek: content that can be updated',headline:'CONTENT THAT CAN CHANGE',points:['Services','Pricing','Articles'],project:'kierunek',caption:'A website launch is not the end of its content. Service descriptions, prices and articles can change after the design is finished.\n\nThe Kierunek psychology centre portfolio demo includes Sanity for managing content. It is a useful starting point for discussing which parts of a website a business needs to update regularly.\n\nBefore planning a content management system, list the information that changes and who will prepare it. That list helps define the editing work the system needs to support.\n\nExplore this portfolio demo at mindvortex.pro/en.'},
 '2026-09-16':{topic:'Keeping old links useful during a rebuild',headline:'GIVE OLD LINKS A HOME',points:['List your existing URLs','Map useful replacements','Check the redirects'],project:'none',caption:'A website rebuild can change page addresses. People may still arrive through an old bookmark, a shared link or a search result.\n\nBefore replacing the site, keep a list of its existing URLs. Where a page has a relevant replacement, plan a redirect to that destination. Sending every old address to the homepage can leave visitors searching again.\n\nAfter launch, test the important old links and check where they lead. Include service pages and links used in emails or printed materials.\n\nSave this check for your next website rebuild.'},
 '2026-09-18':{topic:'FLC: browsing an inventory',headline:'FROM BROWSING TO DETAIL',points:['Browse the inventory','Compare what matters','Explore a vehicle'],project:'flc',caption:'An automotive website needs to help visitors explore more than a single vehicle photograph. The FLC luxury automotive portfolio demo includes an interactive inventory and a scroll-led vehicle presentation.\n\nThese represent different moments in browsing: exploring what is available, then looking more closely at a particular vehicle. Thinking about those moments helps define what information belongs in a collection and what belongs on a detail view.\n\nThis is a design and development demonstration, not a claim about sales results. Explore the portfolio demo at mindvortex.pro/en.'}
};
try{
 // Move all affected drafts to new topics before comparison, so discarded copy cannot block a replacement.
 for(const [day,change]of Object.entries(replacements)){const p=(await pool.query('SELECT * FROM social_posts WHERE day=$1',[day])).rows[0];if(p.status!=='draft')throw Error('Unexpected state');const c=validate({...p.content,...change,alt:'Mind Vortex studio note. '+change.headline+'. '+(change.project==='none'?change.points.join('. '):change.project+' portfolio demo screenshot.')});const image=await render(c,crypto.randomUUID());await pool.query('UPDATE social_posts SET content=$2,image=$3,error=NULL WHERE id=$1',[p.id,c,image]);}
 for(const day of Object.keys(replacements)){const p=(await pool.query('SELECT * FROM social_posts WHERE day=$1',[day])).rows[0];try{await requireDistinct(p.content,p.id);await pool.query("UPDATE social_posts SET status='approved' WHERE id=$1",[p.id]);console.log(day,'Distinct, approved');}catch(e){await pool.query('UPDATE social_posts SET error=$2 WHERE id=$1',[p.id,e.message]);console.log(day,e.message);}}
}finally{await pool.end();}
