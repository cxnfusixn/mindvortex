'use client';
import {useState} from 'react';
export default function Login(){const [step,setStep]=useState('login'),[value,setValue]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);
 async function submit(e){e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/studio-social/api/auth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:step,...(step==='login'?{password:value}:{code:value})})});const d=await r.json();if(!r.ok)throw Error(d.error||'Spróbuj ponownie.');if(step==='login'){setStep('verify');setValue('');}else location.assign('/studio-social');}catch(e){setError(e.message);}finally{setBusy(false);}}
 return <main style={{maxWidth:600}}><a className="brand" href="https://mindvortex.pro">MIND VORTEX<span>SOCIAL STUDIO</span></a><section className="intro"><h1>Panel studia.</h1><p>{step==='login'?'Wpisz hasło administratora. Następnie wyślemy kod na adres e-mail właściciela.':'Kod wysłany na adres e-mail właściciela. Jest ważny przez 10 minut.'}</p></section><form onSubmit={submit}><label htmlFor="credential">{step==='login'?'Hasło':'Kod z e-maila'}</label><input id="credential" style={{display:'block',width:'100%',margin:'12px 0 20px'}} type={step==='login'?'password':'text'} autoComplete={step==='login'?'current-password':'one-time-code'} inputMode={step==='login'?'text':'numeric'} maxLength={step==='login'?256:6} required value={value} onChange={e=>setValue(e.target.value)}/>{error&&<p role="alert" className="error">{error}</p>}<button disabled={busy}>{busy?'Proszę czekać…':step==='login'?'Wyślij kod':'Zaloguj się'}</button>{step==='verify'&&<button type="button" onClick={()=>{setStep('login');setValue('');}}>Zacznij ponownie</button>}</form></main>;
}
